spring:
  cloud:
    iot:
      platform: RASPBERRY
      gpio:
        pins: 
          17:
            dimmedLed:
              enabled: true
          18:
            dimmedLed:
              enabled: true
      raspberry:
        numberingScheme: BROADCOM
